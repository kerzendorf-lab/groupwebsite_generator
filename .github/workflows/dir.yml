name: Dir

on:
  workflow_dispatch:

jobs:
    list_files:
        runs-on: ubuntu-latest
        
        steps:
        - name: Checkout repository
          uses: actions/checkout@v2
        
        - name: List files
          run: |
            ls -R
    